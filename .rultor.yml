architect:
  - amihaiemil
docker:
  as_root: true
install: |-
  sudo apt-get update
  curl -sL https://deb.nodesource.com/setup_7.x | sudo -E bash -
  sudo apt-get install -y nodejs
  sudo apt-get install jq
  sudo node --version
  whereis npm
  pwd
  sudo npm install charles-search-box
  ./node_modules/.bin/webpack --version
decrypt:
  deployment.txt: "repo/dcfg/deployment.txt.asc"
merge:
  script: |-
    NODE_ENV=production ./node_modules/.bin/webpack
deploy:
  script: |-
    NODE_ENV=production ./node_modules/.bin/webpack
    chmod +x ./deploy.sh
    ./deploy.sh
release:
  script:
  - echo "Any release script here..."
